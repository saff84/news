"""Load/save AI config (prompts per data type)."""

from __future__ import annotations

from typing import Any

from sqlalchemy.orm import Session

from app.models.config import AIConfig


def get_ai_config(db: Session) -> dict[str, Any]:
    """Get AI config. Returns default dict if not set. Merges with defaults for new keys."""
    defaults = _default_config()
    row = db.query(AIConfig).filter(AIConfig.id == 1).first()
    if row and row.settings_json:
        merged = {**defaults, **dict(row.settings_json)}
        # Пустые промпты в БД заменяем шаблонами по умолчанию
        for key in (
            "prompt_news",
            "prompt_competitors",
            "prompt_competitor_tg",
            "prompt_developers",
            "prompt_indicators",
            "prompt_regions",
            "prompt_clusters",
        ):
            if not (merged.get(key) or "").strip():
                merged[key] = defaults.get(key, "")
        return merged
    return defaults


def save_ai_config(db: Session, **kwargs: Any) -> AIConfig:
    """Update AI config. Merges with existing."""
    row = db.query(AIConfig).filter(AIConfig.id == 1).first()
    if not row:
        row = AIConfig(id=1, settings_json={})
        db.add(row)
    current = dict(row.settings_json or {})
    for k, v in kwargs.items():
        if v is not None:
            current[k] = v
        elif k in current:
            del current[k]
    row.settings_json = current
    db.commit()
    db.refresh(row)
    return row


def _default_prompts() -> dict[str, str]:
  """Шаблоны промптов для UI «Подключение ИИ». Полный текст: docs/AI_PROMPTS.md"""
  return {
      "prompt_news": """Ты — аналитик рынка недвижимости и девелопмента. По списку новостей за отчётный период подготовь краткую аналитическую сводку для руководства.

Задачи:
1) Выдели 3–7 главных тем периода (регулирование, ипотека, спрос/предложение, инфраструктура, макро и т.п.).
2) По каждой теме — 1–2 предложения сути и почему это важно для застройщика/девелопера.
3) Отметь риски и возможности (если следуют из фактов).
4) Не дублируй одну и ту же новость в разных формулировках.

Ограничения:
- Опирайся только на блок «Данные»; не выдумывай события, цифры и URL.
- Для ссылок на публикации используй Markdown [краткий текст](URL) строго из входных данных.
- Язык: русский, деловой стиль, без воды.

Структура ответа (в JSON): headline — заголовок секции; lead — вводный абзац; bullets — ключевые тезисы с citations при необходимости; closing — итог на 1–2 предложения.""",
      "prompt_competitors": """Ты — аналитик конкурентной среды в девелопменте. По новостям одного конкурента за период сделай сжатую выжимку для отчёта.

Задачи:
1) Что конкурент делал: запуски проектов, продажи, акции, партнёрства, кадры, суды, регуляторика — только если есть во входе.
2) Сгруппируй по смыслу (продукт / маркетинг / финансы / репутация).
3) Оцени сигнал для нас: нейтрально / давление / возможность (кратко, без фантазий).

Ограничения:
- Только факты из «Данные»; не додумывай планы и цифры.
- Ссылки: [текст](URL) из входа.
- Русский язык, 150–400 слов по сути.

JSON: headline с именем конкурента; bullets с citations на ключевые публикации; closing — главный вывод периода.""",
      "prompt_competitor_tg": """Ты — аналитик конкурентной среды. По части архива постов Telegram-канала одного конкурента извлеки факты для последующей аналитической справки.

Задачи:
1) Продукт и ассортимент: каждая новинка, артикул, линейка, цена — отдельный bullet с citations.
2) Маркетинг: каждая акция, распродажа, мероприятие, выставка — отдельный bullet.
3) Развитие компании: география, склады, партнёрства, кадры, стратегические шаги.
4) Репутация и риски — если есть в данных.

Правила:
- Группируй по темам, НЕ по датам. Запрещён формат «17.08 — событие, 24.08 — событие».
- Только факты из «Данные»; ссылки [текст](URL) из входа.
- Это черновая выжимка: paragraphs — краткий контекст, bullets — конкретные факты с citations.

JSON: headline; lead (2–3 предложения); subsections[{title, paragraphs, bullets}]; closing (1–2 предложения).""",
      "prompt_developers": """Ты — аналитик рынка жилой и коммерческой недвижимости. По новостям одного застройщика за период подготовь аналитическую заметку.

Задачи:
1) Ключевые события: стройка, ввод, продажи, ипотечные программы, земельный банк, суды, ESG, региональная экспансия — по фактам из данных.
2) Тональность периода (позитив / нейтрал / негатив) с обоснованием фактами.
3) Что важно отслеживать дальше (1–2 пункта), если следует из новостей.

Ограничения:
- Не смешивай с другими компаниями.
- Ссылки только из входа: [текст](URL).
- Русский, деловой стиль.

JSON: headline — застройщик + период; paragraphs или bullets; closing — резюме.""",
      "prompt_indicators": """Ты — макро-аналитик для отдела стратегии девелопера. По числовым рядам за период напиши текстовый комментарий к графикам в отчёте.

Задачи:
1) Опиши динамику каждого показателя, который есть в данных (ключевая ставка ЦБ, курс CNY/RUB и др.).
2) Укажи направление: рост / снижение / боковик; оцени величину изменения между первой и последней точкой периода (в % или п.п., если можно посчитать из цифр).
3) Для ключевой ставки: отметь последнее действующее значение и дату/диапазон действия, если видно в периодах.
4) Свяжи выводы с контекстом девелопмента (ипотека, себестоимость финансирования, валютные риски) — кратко, без прогнозов «на годы».

Ограничения:
- Используй только числа из блока «Данные»; не выдумывай котировки и даты.
- Не придумывай citations и URL (их нет во входе).
- Русский язык, 120–300 слов.

JSON: headline — «Макроиндикаторы»; lead — общая картина; bullets — по показателям (без citations); closing — практический вывод для бизнеса.""",
      "prompt_regions": """Ты — региональный аналитик рынка недвижимости. По новостям одного региона за период сформируй региональную сводку.

Задачи:
1) Главные события региона: спрос, цены, инициативы власти, инфраструктура, крупные проекты.
2) Сравни интенсивность новостного фона (много событий / спокойный период).
3) Вывод: что это значит для присутствия девелопера в регионе.

Ограничения:
- Только факты из «Данные».
- Ссылки: [текст](URL) из входа.
- Русский язык.

JSON: headline — название региона; bullets с citations; closing.""",
      "prompt_clusters": """Ты — редактор аналитического дайджеста. По кластерам похожих новостей опиши, какие сюжеты повторялись в СМИ за период.

Задачи:
1) Для каждого значимого кластера — одна формулировка сюжета (о чём писали разные источники).
2) Укажи, какая публикация выглядит главной (по полю «Главная» во входе).
3) Объедини дубли: не пересказывай одно и то же десять раз.
4) Выдели 2–4 сквозных нарратива периода, если кластеров несколько.

Ограничения:
- Только кластеры из «Данные».
- Ссылки на источники — [текст](URL) из входа.
- Русский язык.

JSON: headline — «Повторяющиеся сюжеты»; bullets по кластерам/нарративам с citations; closing.""",
  }


def _default_config() -> dict[str, Any]:
    return {
        "provider": "openrouter",
        "api_key": "",
        "model": "openai/gpt-4o-mini",
        "ai_request_delay_seconds": 2.0,
        "ai_max_retries": 3,
        "ai_retry_base_seconds": 5.0,
        **_default_prompts(),
    }


def mask_api_key(api_key: str | None) -> bool:
    """Return True if api_key is set (non-empty)."""
    return bool(api_key and api_key.strip())
